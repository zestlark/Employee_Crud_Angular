import { SpaceTextPipe } from './space-text.pipe';

describe('SpaceTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceTextPipe();
    expect(pipe).toBeTruthy();
  });
});
